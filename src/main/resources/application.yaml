spring:
  datasource:
    # Formato oficial para ler a DATABASE_URL do Render injetando o prefixo JDBC do Java
    url: jdbc:${DATABASE_URL}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

